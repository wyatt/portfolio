export interface GradientColors {
  colors: { color: string; position: string }[];
}

export interface CurrentSongType {
  id: string;
  name: string;
  artist: string;
  href: string;
  album_cover: string;
}
